all:
  children:
    web_servers:
      hosts:
        server1:
          ansible_host: <WEB_SERVER_1_IP_OR_HOSTNAME>
        server2:
          ansible_host: <WEB_SERVER_2_IP_OR_HOSTNAME>
    db_servers:
      hosts:
        server3:
          ansible_host: <DB_SERVER_1_IP_OR_HOSTNAME>

